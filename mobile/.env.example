# Backend API base URL (no trailing slash, no /api suffix - routes are mounted at root, e.g. /auth/login).
# The desktop app and backend both live at https://app.nexoradsa.org (Railway).
EXPO_PUBLIC_API_URL=https://app.nexoradsa.org/api
