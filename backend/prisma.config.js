// Unused: this project runs Prisma 5.x, which reads prisma/schema.prisma
// directly and does not support the prisma.config.js file format
// (that requires @prisma/config, a Prisma 6/7 feature).
// Kept as an empty file so nothing breaks trying to load it;
// safe to delete this file manually.
module.exports = {};
